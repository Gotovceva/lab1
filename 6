#Лабараторная работа № 2
#Уровень 3, задача 2
#В компьютер вводится по очереди координаты n точек. Определить сколько из них попадают в круг с радиусом r с центром в точке с координатами (a,b). Решить задачу для случая, когда количеств точек неизвестно.
import math
def curcle(x,y,a,b,r):
    d = (x-a)**2+(y-b)**2
    return d <= r**2
a = float(input("Введите координату a центра круга "))
b = float(input("Введите координату b центра круга "))
r = float(input("Введите радиус круга "))
count = 0
while True:
    try:
        x = float(input("Введите кординату х точки или букву для выхода: "))
        y = float(input("Введите кординату y точки или букву для выхода: "))
        if curcle(x,y,a,b,r) == True:
            count += 1
    except:
        print("Выход из программы")
        break
print(f"Количество точек, попавших в круг: {count} ")
